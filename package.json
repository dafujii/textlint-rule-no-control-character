{
  "name": "textlint-rule-no-control-character",
  "description": "textlint rule that disallow to use control character.",
  "author": "dafujii",
  "version": "1.0.1",
  "license": "MIT",
  "keywords": [
    "textlintrule"
  ],
  "main": "lib/textlint-rule-no-control-character.js",
  "files": [
    "lib/",
    "src/"
  ],
  "scripts": {
    "test": "textlint-scripts test",
    "build": "textlint-scripts build",
    "prepublish": "npm run --if-present build",
    "watch": "textlint-scripts build --watch"
  },
  "devDependencies": {
    "@textlint/types": "^1.2.3",
    "@types/node": "^12.12.14",
    "textlint-scripts": "^3.0.0",
    "textlint-tester": "^5.1.11",
    "ts-node": "^8.5.4",
    "typescript": "^3.6.4"
  },
  "dependencies": {
    "match-index": "^1.0.3"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/dafujii/textlint-rule-no-control-character"
  }
}
